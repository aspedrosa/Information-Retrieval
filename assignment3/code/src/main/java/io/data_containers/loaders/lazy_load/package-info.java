/**
 * Classes that load data containers in a lazy way.
 * They make use of an iterator to read one entry at a time.
 * This strategies are used just for the indexing pipeline
 */
package io.data_containers.loaders.lazy_load;